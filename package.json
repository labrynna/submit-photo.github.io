{
  "name": "submit-photo",
  "version": "1.0.0",
  "description": "A web application for uploading construction site photos with OCR and Google Sheets integration",
  "private": true,
  "scripts": {
    "build": "node generate-config.js"
  },
  "dependencies": {
    "google-auth-library": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
